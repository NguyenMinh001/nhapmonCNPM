public class TaskFactory {
    public static JSONObject createTask(String title, String description, String dueDateStr,
                                        String priority, boolean isRecurring) {
        // sao chép nội dung từ createTask()
    }
}
